// Este arquivo não é mais necessário para a aplicação
// Mantemos apenas um stub vazio para compatibilidade com outros componentes que possam referenciá-lo

export const setupAdminUser = async () => {
  // Função simplificada sem lógica de autenticação
  return {
    success: true,
    message: 'Configuração concluída'
  };
};
